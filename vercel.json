{
  "framework": null,
  "installCommand": "python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt",
  "buildCommand": ". .venv/bin/activate && mkdocs build",
  "outputDirectory": "site"
}